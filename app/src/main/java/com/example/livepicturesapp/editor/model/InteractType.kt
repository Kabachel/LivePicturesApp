package com.example.livepicturesapp.editor.model

/**
 * Types for interacting with drawing area
 *
 * @property Draw Drawing paints on canvas
 * @property Move Moving area on canvas
 * @property Erase Deleting paints on canvas
 */
enum class InteractType {
    Draw,
    Move,
    Erase,
}